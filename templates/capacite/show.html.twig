{% extends 'base.html.twig' %}

{% block title %}Capacite{% endblock %}

{% block body %}
    <h1>Capacite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ capacite.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ capacite.description }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ capacite.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('capacite_index') }}">back to list</a>

    <a href="{{ path('capacite_edit', {'id': capacite.id}) }}">edit</a>

    {{ include('capacite/_delete_form.html.twig') }}
{% endblock %}