{% extends 'base_back.html.twig' %}

{% block title %}Faction index{% endblock %}

{% block body %}
    <h1>Faction index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>NomFaction</th>
                <th>TypeHero</th>
                <th>Description</th>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for faction in factions %}
            <tr>
                <td>{{ faction.nomFaction }}</td>
                <td>{{ faction.typeHero }}</td>
                <td>{{ faction.description }}</td>
                <td>{{ faction.id }}</td>
                <td>
                    <a href="{{ path('faction_show', {'id': faction.id}) }}">show</a>
                    <a href="{{ path('faction_edit', {'id': faction.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('faction_new') }}">Create new</a>
{% endblock %}