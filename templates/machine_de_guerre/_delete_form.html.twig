<form method="post" action="{{ path('machine_de_guerre_delete', {'id': machine_de_guerre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ machine_de_guerre.id) }}">
    <button class="btn">Delete</button>
</form>