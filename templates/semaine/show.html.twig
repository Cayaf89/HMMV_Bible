{% extends 'base_back.html.twig' %}

{% block title %}Semaine{% endblock %}

{% block body %}
    <h1>Semaine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ semaine.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ semaine.description }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ semaine.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('semaine_index') }}">back to list</a>

    <a href="{{ path('semaine_edit', {'id': semaine.id}) }}">edit</a>

    {{ include('semaine/_delete_form.html.twig') }}
{% endblock %}