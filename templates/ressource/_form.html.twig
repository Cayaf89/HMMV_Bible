{{ form_start(form, { 'attr': {'class': 'form-horizontal','id':'popup-validation'} }) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}